export enum EPATHS {
  SERVER_ERROR = "/server_error",
  NOT_FOUND = "/not_found",
  LOGIN = "/login",
  REGISTER = "/register",
}

export enum EERRORS {
  SERVER_ERROR = "server_error",
  NOT_FOUND = "not_found",
}
